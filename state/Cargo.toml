[package]
name = "state"
version = "0.1.0"
edition = "2021"

[dependencies]
gstd = "1.1.0"
gmeta = { version = "1.1.0", features = ["codegen"] }
io = { path = "../io" }

[build-dependencies]
gear-wasm-builder = "1.1.0"